{
  "name": "ralph-loop-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs && playwright test",
    "test:unit": "node --test test/*.test.mjs",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
